import React from 'react';
import { Grid } from 'semantic-ui-react';

const Footer = () => (
  <Grid.Row style={{ height: '30px', padding: '20px' }}>
    <Grid.Column width={16} textAlign={'center'}>
      <p>footer</p>
    </Grid.Column>
  </Grid.Row>
);

export default Footer;
